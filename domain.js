let pronoun = ['the', 'our'];
let adj = ['great', 'big'];
let noun = ['jogger', 'racoon'];

pronoun.forEach(articulo => {
    
    adjetivo.forEach(adj => {
        sujeto.forEach(suj => {
            console.log(articulo + adj) + ".com";
        });
    });

});